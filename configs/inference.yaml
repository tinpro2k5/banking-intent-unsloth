#base_model: "unsloth/Llama-3.2-3B-Instruct"
adapter_path: "checkpoints/20260424_122036"
label_text_map: "configs/label_text_map.json"
used_label_text_map: "configs/used_label_text_map.json"
max_seq_length: 128
load_in_4bit: true
max_new_tokens: 16
chat_template: "llama-3"
dtype: null # Defaults to None; use torch.float16 or torch.bfloat16 for newer GPUs.
# Same as train.yaml; ensure consistency between training and inference configs.

base_model_max_seq_length: 512 # for evaluation, pretrained models use system prompts that can be long
